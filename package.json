{
  "name": "md-to-html-pipeline",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Markdown -> styled HTML pipeline matching the Daily Brief design.",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "build:watch": "node build.js --watch"
  },
  "dependencies": {
    "marked": "^14.1.3"
  }
}
